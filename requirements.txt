streamlit>=1.28.0
numpy>=1.24.0
pandas>=2.0.0
Pillow>=10.0.0
typing-extensions>=4.5.0

# CRITICAL: OpenCV headless for cloud deployment (no GUI dependencies)
# This MUST be installed before ultralytics to prevent opencv-python installation
opencv-python-headless==4.11.0.86  # pin exact headless build to satisfy ultralytics without GUI deps

# YOLO model dependencies
# Note: ultralytics may try to install opencv-python, but opencv-python-headless
# should satisfy the opencv requirement
ultralytics==8.3.236
torch==2.9.1
torchvision==0.24.1
scikit-image>=0.21.0

# Matplotlib with non-GUI backend
matplotlib>=3.7.0
seaborn>=0.12.0
